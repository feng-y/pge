# Preflight Handoff

This file is intentionally not part of the current executable lane.

Current execution uses the resident Planner / Generator / Evaluator progression plus the bounded same-contract `generator <-> evaluator` repair loop.

Keep this file on disk as an archived design seam for future experimentation.
Do not load it during normal `/pge-execute` runs.
