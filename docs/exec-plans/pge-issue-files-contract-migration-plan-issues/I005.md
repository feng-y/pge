# I005: Add Protocol Consistency Coverage

Intent: make the migration verifiable and prevent drift across docs, templates, gates, and skills.

## introduction

Add eval/manual protocol coverage for the issue-file happy path, missing issue files, embedded-plan rejection, and exec-local adaptation inside plan boundaries.

## evaluation

This is integration proof. It should run after producer, validator, consumer, and evaluator/review boundaries are updated so coverage reflects the coordinated contract rather than a partial migration.

## dependency_order

- Order: 5
- Before: none
- After: I001, I002, I003, I004

## status

- State: READY_FOR_EXECUTE
- Dependencies: I001, I002, I003, I004
- Verification Coupling: integration-only
- Execution Type: AFK
- Security: no

## context

- Plan: ../pge-issue-files-contract-migration-plan.md
- Requirement goal served: cross-surface proof that the coordinated migration is consistent and verifiable.
- Upstream decisions: coverage tests the protocol contract and does not introduce a new workflow surface.

## task

Add eval/manual protocol coverage and final consistency checks that prove producer, validator, consumer, evaluator/review, and run-state boundaries agree on the new issue-file plan contract.

## behavior_contract

- Current Behavior: evals may not distinguish embedded issue-body plans from issue-file plans.
- Desired Behavior: evals/protocol checks cover issue-file happy path, missing referenced issue file, embedded-plan rejection, and exec-local adaptation without plan mutation.
- Behavior Delta: add scenarios and grep/manual checks that detect drift across docs, templates, gates, exec handoffs, evaluator/review wording, and run-state boundary.
- Key Interfaces: `skills/pge-plan/evals/evals.json`, possibly `skills/pge-plan/evals/joint-evals.json`, and all docs touched by I001-I004.
- Trigger Predicate: after I001-I004 changes are applied.
- Output Admission Predicate: final evidence shows no producer/validator/consumer/evaluator mismatch and no stale embedded-executable wording remains.
- Out Of Scope Confirmed: do not create a new workflow surface or require broad runtime implementation outside named files.
- What Not To Infer: grep alone is not sufficient proof; include manual protocol interpretation.

## scope

### Do

- Add or update eval scenarios for the new contract.
- Run grep/manual consistency checks across producer, validator, consumer, evaluator/review, and run state boundaries.
- Record final protocol consistency result.

### Do Not

- Do not add new task-level execution state.
- Do not expand route/status/verdict vocabulary beyond `non_canonical_plan_shape` as an exec failure reason.
- Do not introduce a new agent or scheduler authority.

## target_areas

- `skills/pge-plan/evals/evals.json`
- possibly `skills/pge-plan/evals/joint-evals.json`
- docs references touched by earlier issues

## behavior_delta

- Current behavior: evals may not distinguish embedded issue-body plans from issue-file plans.
- Desired behavior: evals/protocol checks cover producer output, gate blocking behavior, exec rejection route, and local adaptation inside plan boundaries.
- Do not change: evals should test the contract, not introduce a new workflow surface.

## acceptance

- Eval scenario covers a large clear plan that must externalize issue files.
- Eval scenario covers missing referenced issue file blocking execution.
- Eval scenario covers embedded issue bodies routing upstream for contract upgrade instead of executing.
- Eval scenario covers exec adaptation of issue-local imperfections without plan mutation.

## validation

- Manual eval JSON/schema inspection.
- Grep for inconsistent artifact path vocabulary.
- Run available eval/schema checks if the repo exposes them; otherwise record why manual inspection is the strongest available validation.

## local_validation

- `rg -n "embedded issue|issue file|non_canonical_plan_shape|plan_context_packet|exec/progress|decision-log" README.md README-CN.md CLAUDE.md skills`
- Inspect eval JSON for happy path, missing file, embedded-plan rejection, and exec-local adaptation scenarios.
- Compare producer, validator, consumer, evaluator/review, state writer, and final response wording manually.

## required_evidence

- eval scenario names or summaries
- grep summary for stale embedded-executable wording
- final protocol consistency result across producer, validator, consumer, evaluator, and run state boundaries

## risks

- Coverage could lag behind docs and allow protocol drift to recur.
- A narrow grep could miss semantic disagreement between surfaces.

## source_refs

- source_plan: `../pge-issue-files-contract-migration-plan.md`
- dependencies: `I001.md`, `I002.md`, `I003.md`, `I004.md`
- accepted_direction: `../../../codex_plan.md`
